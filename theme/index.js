import theme from "styled-theming";

export const screen = theme("theme", { light: "#FFFFFF", dark: "#111216" });
export const basic = theme("theme", { light: "#FFFFFF", dark: "#0F101400" });
export const textColor = theme("theme", { light: "#262626", dark: "#b7b7b7" });
export const subTextColor1 = theme("theme", {
  light: "rgba(0,0,0,0.3)",
  dark: "#BDB5B5",
});
export const btnColor = theme("theme", {
  light: "#2F80ED",
  dark: "#2F7EE8",
});
export const borderColor = theme("theme", {
  dark: "#97BEF4",
  light: "#BBD8FF",
});
export const shadowColor = theme("theme", {
  light: "0px 4px 12px rgba(0, 0, 0, 0.1)",
  dark: "0px 4px 12px rgba(67, 67, 67, 0.1)",
});
export const dropzone = theme("theme", { light: "#F6F8FB", dark: "#2F3B4D" });
